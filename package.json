{
  "name": "test-waveson-api",
  "private": true,
  "proxy": "http://localhost:3001",
  "dependencies": {},
  "scripts": {
    "build": "cd waveson && yarn install && yarn build && cd ..",
    "deploy": "cp -a waveson/build/. public/",
    "postinstall": "yarn build && yarn deploy"
  },
  "engines": {
    "yarn": "1.22.4",
    "node": "12.13.1"
  }
}
